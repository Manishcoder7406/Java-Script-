// primitive 

const score = 100
const scoreValue = 100.33   // in js decimal values and float values are considered as numbers #same //


const isLoggedin = false
const outsidetemp = null
let userEmail;

const id= Symbol('123')
const anotherId = Symbol('123')

console.log(id === anotherId);